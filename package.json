{
    "name": "myportfolio-monorepo",
    "version": "1.0.0",
    "scripts": {
        "install-backend": "cd backend && npm install",
        "build": "npm run install-backend && node backend/deploy_prepare.js",
        "start": "cd backend && npm start"
    },
    "engines": {
        "node": "20.x"
    }
}